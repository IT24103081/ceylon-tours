{
  "name": "ceylon-tours",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5173 --open=index.html",
    "start": "live-server --port=4173 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
